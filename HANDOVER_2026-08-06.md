# Handover — 2026-08-06 00:xx EEST

Supersedes `HANDOVER_2026-08-05.md` for current state. Read this first.
Earlier handovers are still useful for design rationale on the labeling
rework — keep them, don't delete.

**TL;DR**: This session was cut short by a scheduled 1:55am power outage
(known in advance, not a surprise crash). Rather than race the clock,
scope was deliberately narrowed to what could be finished safely: the
val→test-gap finding (already fully investigated as of 2026-08-05 ~22:15,
see `HANDOVER_2026-08-05.md` §1) is now written into the actual paper
sources, the real Overleaf `.tex` sources for both loose PDFs at repo root
were recovered from zips that were sitting at repo root the whole time,
and everything is committed and pushed. The planned 3-way encoding
comparison (primary vs. validated-bytedump vs. validated-flowpic) is
**not done** — the bytedump leg was killed mid-training, undertrained, and
deliberately excluded from the paper rather than reported misleadingly.

---

## 1. What's actually in the paper now

Both `research/main.tex` and `research/synshield_paper/paper.tex` (see §2
below for what that second path is) have a new section/subsection
presenting:

- The cluster-validated labeling pipeline (k-means + flood-signature gate)
  replacing the raw SYN-count heuristic, paired with a FlowPic-style
  encoding.
- A stricter cross-day held-out split (entire `01-12-2018` attack day held
  out, not just unseen windows).
- The honest result: validation `f1_ddos=0.866`, cross-day test
  `f1_ddos=0.419` raw / `0.453` after correcting 86 confirmed mislabels
  (verified against CICFlowMeter's official per-flow labels, independent
  ground truth).
- (`research/main.tex` only, cut from the paper for space) the two
  unsupervised baselines (autoencoder, Deep SVDD) as a reported negative
  result — both underperform because DDoS windows are sparser, not busier,
  than normal in this representation, inverting the anomaly-detection
  premise both methods rely on.

Full derivation: `reports/label_validation_and_generalization_gap_2026-08-06.md`
(standalone, citable version of the finding) and `WORK_LOG.md`'s
2026-08-04/05 entries (the investigative blow-by-blow).

**Not verified by compiling** — no `pdflatex`/`texlive` on this machine.
Did a manual brace/environment-balance sanity check (see `WORK_LOG.md`'s
2026-08-05 ~23:35 EEST entry for the method); pre-existing imbalance in
`research/main.tex` confirmed to predate this session's edits, not
introduced by them. **Recommend an actual compile pass (Overleaf or local
`pdflatex`) before treating either document as submission-ready.**

## 2. The recovered Overleaf sources — read this if confused about which file is which

There are now genuinely **four** paper-like things in this repo. Don't
conflate them:

1. `research/main.tex` — the 28-page full report. Existing, tracked,
   unchanged in identity by this session (only content edits, see §1).
2. `research/synshield_paper/` — **new this session**. The real SYNShield
   IEEE conference paper (`paper.tex` + `IEEEtran.cls`/`.bst` +
   `references.bib` + figures), extracted from
   `DDoS_Attack_Detection_Using_ResNeXt50_32x4d_with_MindSpore___paper.zip`
   (which was sitting at repo root, unexamined, since at least 2026-07-26 —
   the earlier session's "find the Overleaf sources" open question was
   answerable the whole time, just not looked at). Real author block:
   Nehme, Al Ghoush, Farhat. Farhat's email is still a placeholder
   (`XXXXXXX@bau.edu.lb`) — **unresolved, needs the real address, not
   something to guess**.
3. `ict_innovation_overleaf/main.tex` — existing, tracked, **a different,
   unrelated competition submission** (still templated: `\TeamName`
   placeholder, no real title/author). Not touched this session, not the
   same document as #2 despite the superficially similar directory name.
   Don't confuse the two.
4. The two loose PDFs + two zips at repo root
   (`DDoS_Attack_Detection_Using_ResNeXt50_32x4d_with_MindSpore.{pdf,zip}`,
   `..._paper.{pdf,zip}`) — left in place, not deleted. The `.zip` for the
   report is near-identical to `research/main.tex` (abstract wording only);
   the `.zip` for the paper is now tracked properly at `research/synshield_paper/`.
   The loose files at root are now redundant but harmless — fine to clean up
   later, not urgent.

## 3. The dropped 3-way comparison

`model/per_second_0p3_validated_v1/` (bytedump encoding, cluster-validated
labels) was relaunched fresh at 23:19 EEST after the night's earlier
(5th) power loss, but only reached ~epoch 4 before being **deliberately
killed** at ~23:55 once the 1:55am scheduled-outage math made it clear it
wouldn't reach the ~epoch-16+ point this recipe needs to stabilize (see
`WORK_LOG.md`'s flowpic run history — same recipe took until epoch 16-22
there). User + Claude agreed explicitly: don't put an undertrained
checkpoint's number in the paper as if it were a finished ablation leg.

**To resume**: the dataset split is already done and correct
(`dataset/images_per_second_window_0p3_validated_v1/{train,val,test}`,
verified matching the flowpic split's stem-level assignment exactly).
Just relaunch `scripts/train_resnext_per_second_0p3_validated_v1.sh` fresh
with no time pressure, let it run to natural early-stopping, then:

```bash
HUAWEI_EVAL_ALLOW_NON_PRIMARY_CKPT=1 .venv/bin/python scripts/eval_test_detailed.py \
  --ckpt model/per_second_0p3_validated_v1/resnext50_32x4d_best.ckpt \
  --data-root dataset/images_per_second_window_0p3_validated_v1 \
  --eval-root dataset/images_per_second_window_0p3_validated_v1 \
  --split test --device-target GPU
```

compare against the primary (`scripts/eval_test_detailed.py` with defaults,
no env var needed) and validated-flowpic
(`--ckpt model/flowpic_0p3_validated_v1_continued/resnext50_32x4d_best.ckpt
--data-root dataset/images_flowpic_0p3_validated_v1 --eval-root dataset/images_flowpic_0p3_validated_v1
--norm-mean 0.0071 0.0003 0.0004 --norm-std 0.0592 0.0157 0.0145
HUAWEI_EVAL_ALLOW_NON_PRIMARY_CKPT=1`) checkpoints, then write the 3-way
comparison into `reports/` and reference it from the paper.

## 4. GitHub push — now confirmed working

Earlier sessions always deferred `git push` to the user manually (sandbox
blocks it without confirmation). Tonight, tested it directly (disposable
branch, pushed, verified, deleted both locally and remotely) and found it
**did not work** — no credential helper configured. Fixed:
`sudo snap install gh --classic` (plain `apt install gh` doesn't exist in
Ubuntu 22.04's default repos at a usable version, and `apt update` itself
fails here on an unrelated pre-existing broken `cudnn-local-repo` GPG
signature — not a new problem, don't chase it) then `gh auth login`
(device-code flow, browser). Confirmed working end-to-end after. This
should still work in future sessions unless the token is revoked.

## 5. Everything else still open (unchanged from before, low priority)

- Mohammed Farhat's placeholder email (`research/synshield_paper/paper.tex`).
- `install.sh` at repo root — believed to be Anthropic's own CLI installer,
  dropped in by accident, not part of this project. Never deleted, still
  sitting there.
- The 3-way encoding comparison (§3 above).
- Whether Deep SVDD is worth one more attempt (random-init encoder instead
  of AE-pretrained) — still just a judgment call, current negative result
  is reported as-is.
